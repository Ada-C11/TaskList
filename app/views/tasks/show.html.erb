
<h1><%= @task.name %> </h1>
<ul>
      <p><%= @task.description %></p><br>
      <% status = "completed" if @task.completed %>
      <% status = "in-progress" unless @task.completed %>
      <p class="status"><Status: <%= status %></p>
      Created: <%= @task.created_at %>
</ul>
