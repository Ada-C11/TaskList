<h2> Task Details</h2>
<article>
  <p class="show-header">
    <%= "#{@task.name}" %>
  </p>
  <p>
    <%= "#{@task.description}" %>
  </p>
  <p>
    <% if @task.completion_date %>
      Completed: <%= @task.completion_date %>
    <% end %>
  </p>
</article>
