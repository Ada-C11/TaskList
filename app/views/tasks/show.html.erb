<h1>Task Details</h1>

<p>
  Task: <%= @task.name.capitalize %>
</p>
<p>
  Description: <%= @task.description.capitalize %>
</p>
<%= link_to "Edit Task", edit_task_path(@task.id) %>
<%= link_to "Delete", task_path(@task.id), method: :delete, data: {confirm: "Are you sure?"} %>

