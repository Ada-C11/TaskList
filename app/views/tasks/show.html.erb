<h2>
    <%= @task.name%>
</h2>

<p>
 <%= @task.description%> 
 <% if @task.current_status == false %>
 <li>Task Pending</li>
 <% else%>
 <li>Task Completed</li>
 <% end %>
</p>

<%= link_to "Back to Tasks List", tasks_path%>

<%#= link_to "Edit #{@task.name}", edit_task_path(@task.id)%>