<%= form_with model: @task, class: form_class do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit "Save Task", class: "submit-button"%>
<% end %>

<%= link_to "Back to all tasks", tasks_path %>
