<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
          <%= link_to task.name, task_path(task.id) %>
      <% if task.completion_date %>
        <s><%= task.name %></s>
      <% else %>
        <%= task.name %>
      <% end %>
    </li>
  <% end %>
