

<p>
    There are <%= @tasks.length %> tasks on the list.
</p>
<p>
    Remember to:
</p>
<ul>
    <% @tasks.each do |task| %>
    <li>
        <%= link_to task.name, task_path(task) %>
        <%= link_to "Edit", edit_task_path(task) %>
    </li>
    <% end %>
</ul>