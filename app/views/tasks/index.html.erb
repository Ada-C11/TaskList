<h2> Tasks </h2>

<ul class="task-list">
    <% @tasks.each do |task| %>
        <li><%= link_to task.title.upcase, task_path(task) %>: <%= task.content%> </li>
      <% end %>
      <li>
      <%= link_to "Add a new task", new_task_path %>
      </li>
</ul>   