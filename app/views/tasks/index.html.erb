
<ul>
    <% @tasks.each do |task| %>
      <li>
          <%= task.name %>: <%= task.description%> ||  Due <%= task.completion_date %>
          || <%= link_to "Edit", edit_task_path(task)%>
      </li>
    <% end %>
</ul>