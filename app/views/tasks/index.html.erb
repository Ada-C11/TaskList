<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to "#{task.name}", task_path(task.id) %>
      <%= link_to "Edit", edit_task_path(task) %>
    </li>
  <% end %>

  <%= link_to "Add a Task", new_task_path%>

</ul>