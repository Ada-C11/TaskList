<h1>Task List</h1>

<ul>
<% @tasks.each do |task| %>
  <li>
    Where: <%= link_to task.name, task_path(task.id) %>

    Date: <%= task.completion_date %>
  </li>
<% end %>
<%= link_to "Create a new Task", new_task_path %>
</ul>