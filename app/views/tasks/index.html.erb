<h1>Tasks</h1>
<ul>
<h2>To Do List:</h2>
<% @tasks.each do |task| %>
<li>
  <%= link_to task.name, task_path(task.id)%>
</li>
  <% end %>

<%= link_to "Add New Task", new_task_path %>
<%# <%= link_to "Edit Task", edit_task_path(task.id), method: :edit %>
<%# <%= link_to "Delete Task", task_path(@task.id), method: :delete %>
</ul>
