<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <strong><%= link_to task.name, task_path(task) %></strong>
      <%= task.description %>
      <%= task.completion_date %>
    </li>
  <% end %>
</ul>