<h1>Shamira's Tasks</h1>
<p>
<% @tasks %>
</p>
<ul>
<% @tasks.each do |task| %>
  <li>
  <!-- fix anchor -->
 <%= link_to task.name, task_path(task.id)%>
    <%= task.name %>,  Details: <%= task.description%>, due <%= task.completion_date%>
   
  </li>
<% end %>
</ul>