<h2>Form for editing a task...</h2>

<%= form_with model: @task, class: "edit-task" do |f| %>
    <p>Please fill out this form to edit a task </p>

    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_field :description %>
    <%= f.submit "Save Task", class: "task-form__submit-btn" %>

<% end %>